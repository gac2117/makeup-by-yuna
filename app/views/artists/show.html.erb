<%= render '/layouts/navbar_signedin' %>

<h1> Welcome to <%= @artist.name %>'s page!</h1>

About <%= @artist.name %>:<br>
<%= @artist.extra %><br>
<br>

<% if @client %>
<%= link_to "Go back", client_path(@client) %> to your profile page.

<% elsif @current_artist %>
<%= link_to "Click here", appointments_path %> to view your appointments.<br><br>
<%= link_to "Sign Out", signout_path, method: :delete %>

<% else %>
<%= link_to "Go back", artist_path(@other_artist) %> to your profile page.
<% end %>
<br><br>
