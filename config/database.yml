# PostgreSQL.
#   gem install pg
#
#   Configure using Gemfile
#   gem 'pg'
#

development:
  adapter: postgresql
  encoding: unicode
  database: makeup_by_yuna_development
  pool: 5
  username: gachoi
  password:
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: unicode
  database: makeup_by_yuna_test
  pool: 5
  username: gachoi
  password:
  timeout: 5000
